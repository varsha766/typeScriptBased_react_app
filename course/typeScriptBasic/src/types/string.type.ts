// typee string
// any value betweent single quote or double quote treated as string type

let fName: string = "varsha";

let fullName: string = firstName + " " + "kumari";

fullName.split("");

// type incompitable
// fullName = 1223456;

// Math.log(fullName);
